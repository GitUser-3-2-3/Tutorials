spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/brownpizza
    username: postgres
    password: qwerty1,0*
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop